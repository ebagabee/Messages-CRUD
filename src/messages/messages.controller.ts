import { Controller } from '@nestjs/common';

@Controller('messages')
export class MessagesController {
  // Route to return all messages
  findAll() {}
  //   Find onde message
  findOneMessage() {}
}
